#include "layers.h"

using namespace std;

vector<int> Layers::t_[2];
map<int, int> Layers::map_;
map<int, int> Layers::map_inv_[2];
int Layers::node_number_;
int Layers::my_layer_;
int Layers::number_of_nodes_;
